# Archived knowledge data

Knowledge V3.2 and the static V4 projection are retained here for audit history only.
They are not copied into the production artifact and must not be used as a runtime fallback.

Production publishes only `public/data/knowledge-v5`. Rollback is performed by switching
to a retained, previously verified release rather than by co-hosting an older knowledge dataset.
